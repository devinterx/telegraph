import React from "react";
import DashboardComponent from "../Dashboard/DashboardComponent";

export default class Scenes extends DashboardComponent {
    render() {
        return (
            <div className={`${DashboardComponent.className} scenes-container`}>

            </div>
        );
    }
}
